# PROJECT ROADMAP (GSD)

## Wave 1: The Clean Slate (Foundation)
- Delete existing component and page structure (`src/components`, `src/pages`).
- Reset `App.jsx` and `index.css`.
- Ensure routing setup is clean.

## Wave 2: Layout & Global Design System
- Integrate `taste-skill-main` dials (tactile backgrounds, material depth, cohesive color scheme).
- Integrate `ui-ux-pro-max` constraints for layout architectures (Bento grids, sticky sidebars).
- Re-create `Subpage.jsx` dynamic template mapping to `abts_content_clean.json`.
- Create a reusable gallery component that reads from `public/assets` and formats the filename as a subtitle.

## Wave 3: Content Porting & Folds
- Build the `Home.jsx` with innovative scroll folds.
- Integrate all navigation links mapping to the JSON subpages.
- Establish the Gallery page.

## Wave 4: Animations & Polish
- Integrate `react-bits-main` components (BlurText, SplitText).
- Apply GSAP ScrollTrigger for fold transitions.
- Verify responsiveness and accessibility.
